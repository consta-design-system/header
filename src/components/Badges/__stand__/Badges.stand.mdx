import { BadgesExampleBasic } from './examples/BadgesExample';

```tsx
import { Badges } from '@consta/header/Badges';
```

Ряд бейджиков, красиво сворачивается, если не помещается по длине в родительский контейнер. Наследует свойства компонента [Badge](##LIBS.LIB.STAND/lib:uikit/stand:components-badge-stable).

<BadgesExampleBasic />

> Этот компонент не встраивается в шапку сам по себе, а используется в [уведомлениях](##LIBS.LIB.STAND/lib:header/stand:components-notifications-stable). Но вы можете использовать его как-то иначе.
